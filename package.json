{
  "name": "otto_test_site",
  "version": "1.1.0",
  "description": "",
  "engines": {
    "node": "10.15.3"
  },
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-dev": "nodemon app.js",
    "start": "node app.js",
    "commit-code": "git add -A && git commit -m 'Release commit for Version 1.1.0' && git push",
    "deploy-to-heroku": "git push heroku master",
    "full_deploy": "npm run commit-code && npm run deploy-to-heroku"
  },
  "author": "Ian Goddard",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "ejs": "^3.0.1",
    "express": "^4.17.1",
    "nodemon": "^2.0.7"
  }
}
